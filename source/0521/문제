#include <stdio.h>

int main(){
    int a,b;
    scanf("%d", &a);
    int VAL[a];
    scanf("%d\n", &b);
    for (int i = 0; i < a; i++)
    {
        scanf("%d", &VAL[i]);
    }
    for (int i = 0; i < a; i++)
    {
        if (VAL[i] < b)
        {
            printf("%d ", VAL[i]);
        }
    }
    
}
